# video-transcript Skill 配置示例
# 复制为 .env 后，把下面替换成你自己的火山方舟 API Key。
# 注意：永远不要把 .env 提交到 GitHub。

DOUBAO_API_KEY=ark-请替换成你自己的APIKey
DOUBAO_MODEL=doubao-seed-2-0-pro-260215

# 可选：默认输出目录。推荐用相对目录，便于在 Obsidian vault 内保存。
VIDEO_TRANSCRIPT_OUTPUT_DIR=Clippings/video-transcripts
